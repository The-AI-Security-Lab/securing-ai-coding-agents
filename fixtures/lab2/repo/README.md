# Synthetic Lab 2 repository

This repository is intentionally small and synthetic. The Stage 2E task may
change only `app/lookup.py`. `tests/`, this file, and `.workshop/` are
protected teaching context.
